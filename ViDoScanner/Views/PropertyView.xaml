<UserControl
  x:Class="ViDoScanner.Views.PropertyView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
  mc:Ignorable="d" 
  x:Name="MainUserControl"
  >
  <UserControl.Template>
    <ControlTemplate TargetType="UserControl">
      <Grid Height="Auto">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="1*"/>
          <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBlock VerticalAlignment="Center" Grid.Column="0" Text="{Binding Text, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"/>
        <ContentPresenter Grid.Column="1" Margin="5,0,0,0"/>
      </Grid>
    </ControlTemplate>
  </UserControl.Template>
</UserControl>
